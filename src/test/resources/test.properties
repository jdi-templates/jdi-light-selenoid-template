driver=${driver}
#drivers.version=2.23
timeout.wait.element=10
domain=${domain}
driver.getLatest=true
#search.element.strategy=strict | soft | visible, multiple | any, single
# the next row should be uncomment, and proper url should be passed in case we use selenoid
driver.remote.url=${remote.url}
smart.locator=#%s
#driver.remote.run=true | false //if not specified, treated as false by default
#page.load.strategy=normal
#browser.size=1800X1000
#drivers.folder=C:\\Selenium
#log.message.format=short | full
#cache=false | true
#demo.mode=false | true
#demo.delay=2
#run.type=local | remote
#screenshot.strategy=on fail | on | off
